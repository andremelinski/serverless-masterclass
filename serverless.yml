service: s3-node-fileupload-serverless
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

# You can pin your service to only deploy with a specific Serverless version
# Check out our docs for more details
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1

# granularity permissions using a plugin
plugins:
  - serverless-iam-roles-per-function

# writing s3 bucket name
custom:
  fileUploadBucketName: ${self:service}-bucket-${self:provider.stage}

# creating the bucket
resources:
  Resources:
    FileBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:custom.fileUploadBucketName}
        AccessControl: PublicRead
        VersioningConfiguration:
          Status: Enabled

functions:
  s3FileUploader:
    handler: src/upload.handler
    name: s3-file-uploader
    description: Lambda that will upload files into S3
    timeout: 60 #seconds
    memorySize: 128 #mb
    # method using from API  Gateway
    events: 
      - http:
          path: images
          method: POST
    # lambda can only add objects into S3 
    iamRoleStatements:
      - Effect: Allow
        Action:
          - "s3:Put*"
        Resource: arn:aws:s3:::${self:custom.fileUploadBucketName}/*
    environment:
      FILE_UPLOAD_BUCKET_NAME: ${self:custom.fileUploadBucketName}
    